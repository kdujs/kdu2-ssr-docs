{
  "name": "kdu2-ssr-docs",
  "version": "1.0.0",
  "private": true,
  "description": "docs for kdu-server-renderer",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && kdupress dev docs",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && kdupress build docs",
    "deploy": "yarn build && firebase deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kdujs/kdu2-ssr-docs.git"
  },
  "keywords": [
    "kdu",
    "ssr",
    "docs"
  ],
  "author": "NKDuy",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kdujs/kdu2-ssr-docs/issues"
  },
  "homepage": "https://github.com/kdujs/kdu2-ssr-docs#readme",
  "devDependencies": {
    "kdupress": "^1.8.0"
  }
}
